{
  "name": "@verevoir/sourced",
  "version": "0.1.0",
  "private": true,
  "description": "Content-addressed source read proxy: tarball-prime, single-flight, persistent. The hosted read path for verevoir/sourced.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5",
    "vitest": "^4"
  },
  "engines": {
    "node": ">=20"
  }
}
